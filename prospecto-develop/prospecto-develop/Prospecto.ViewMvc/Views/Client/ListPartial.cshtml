@using Prospecto.Models.Enums;
@model IEnumerable<Prospecto.Models.ViewModel.ClientViewModel>

<div class="adv-table">
    <table id="tableClient" class="table table-bordered table-hover display nowrap" style="width:100%">
        <thead>
            <tr>
                <th>Código</th>
                <th>Descrição</th>
                <th>Email</th>
                <th>Telefone</th>
                <th>Empresa</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
            <tr style="cursor: pointer">
                <td id="@item.Id" style="width: 10%">  @item.Id.ToString("0000") </td>
                <td style="width: 30%"> @item.Name </td>
                <td style="width: 20%"> @item.Email </td>
                <td style="width: 10%"> @item.Telephone </td>
                <td style="width: 15%"> @item.Company.Description </td>
                <td style="width: 5%">
                    <div class="text-center">
                        <a class="fa fa-pencil" title="Editar" data-id="@item.Id" style="font-size: 16px; color: #75798B" href="@Url.Action("Index", "Client", new { Id = item.Id })"></a>
                        @*<a class="fa fa-remove" title="Excluir" data-id="@item.Id" style="font-size: 16px; color: #75798B" onclick="Excluir($(this))"></a>*@
                    </div>
                </td>
            </tr>
            }
        </tbody>
    </table>
</div>


